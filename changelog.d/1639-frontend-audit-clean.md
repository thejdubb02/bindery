### Security
- **Frontend dependency audit is clean again** (#1639) — `npm audit` reported three high advisories, so the SAST job had been failing on `main` itself for weeks, which trains everyone to ignore a security gate. `react-router-dom` 7.x is replaced by `react-router` 8.3.0 (the DOM package was folded into the core one in v8, and 8.3.0 is the first release outside the advisory range for GHSA-qwww-vcr4-c8h2); the advisory covers RSC mode, which Bindery does not use, so this was a gate failure rather than an exploitable path. The transitive `brace-expansion` advisory in the lint tooling is resolved by a lockfile bump. Router imports moved from `react-router-dom` to `react-router` with no API changes.
